1. 复制 `.env.example` 为 `.env`，填入自己的 Webhook URL。
2. 根据需要修改 `config.json` 中的阈值及路径。
3. 运行 `pip install -r requirements.txt` 安装依赖。
4. 执行 `python monitor.py`。

——————————————————————————————————————————————————

1. Copy `.env.example` to `.env` and fill in your own Webhook URL.
2. Modify the thresholds and paths in `config.json` as needed.
3. Run `pip install -r requirements.txt` to install dependencies.
4. Execute `python monitor.py`.

